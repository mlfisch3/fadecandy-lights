{
  "listen": ["127.0.0.1", 7890],
  "verbose": true,

  "//": "Gamma and whitepoint live HERE, not in the fclights engine.",
  "//dither": "Leave fcserver's own dithering and interpolation enabled; they are the reason this rig uses a Fadecandy at all.",
  "color": {
    "gamma": 2.5,
    "whitepoint": [0.9, 1.0, 1.0]
  },

  "//devices": "One board, OPC channel 0 (broadcast), 512 pixels. To add a second board give each one its own serial and its own OPC channel (1, 2, ...) and match those channels in layout.json.",
  "devices": [
    {
      "type": "fadecandy",
      "map": [
        [0, 0, 0, 512]
      ]
    }
  ]
}
