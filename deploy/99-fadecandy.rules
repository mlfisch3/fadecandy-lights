# Give the plugdev group access to the Fadecandy, so fcserver does not need to
# run as root. 1d50:607a is the Fadecandy's USB vendor and product ID.
SUBSYSTEM=="usb", ATTR{idVendor}=="1d50", ATTR{idProduct}=="607a", MODE="0660", GROUP="plugdev"
